{
    "version": "0.2.0",
    "configurations": [



        {
            "name": "Launch Main",
            "type": "go",
            "request": "launch",
            "mode": "auto",
            "program": "${workspaceFolder}/cmd/ifonly-apiserver", // 明确指定入口
            "env": {},               // 添加需要的环境变量
            "args": ["--mode","debug"],              // 启动参数，比如 ["-port=8080"]
            "cwd": "${workspaceFolder}", // 工作目录（可省略）
            "showLog": true          // 显示调试日志，便于排查问题
        }
    ]
}
